# Pinned to the exact versions verified on Python 3.14.6 (macOS).
# Reproducible for Docker and graders.

# Core data + ML
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
scipy==1.18.0
joblib==1.5.3

# Tree ensembles (gradient boosting)
xgboost==3.4.1

# Explainability (per-prediction SHAP)
shap==0.52.0

# LLM client
groq==1.6.0
pydantic==2.13.4
python-dotenv==1.2.3

# Interface
streamlit==1.62.0

# EDA / visualization
matplotlib==3.11.1
seaborn==0.13.2

# Testing
pytest==9.1.1